<?xml version="1.0" encoding="utf-8"?>
<manifest package="ru.skillbranch.gameofthrones"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".presentation.view.GameOfThronesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameOfThrones">
        <activity
            android:name=".presentation.view.detail.CharacterInfoActivity"
            android:exported="true"
            android:theme="@style/ActivityTheme"/>
        <activity
            android:name=".presentation.view.characters.CharactersActivity"
            android:exported="true"
            android:theme="@style/ActivityTheme" />
        <activity
            android:name=".presentation.view.start.StartActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>